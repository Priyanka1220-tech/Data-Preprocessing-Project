# Data-Preprocessing-Project
Data preprocessing is the process of cleaning and transforming raw data into a usable format for machine learning. It includes handling missing values, removing duplicates, encoding categorical data, and scaling features to improve model accuracy and performance.
